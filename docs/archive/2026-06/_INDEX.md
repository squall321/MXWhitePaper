# 2026-06 Archive Index

| feature | match | date | path |
| --- | --- | --- | --- |
| llm-toolkit-pivot-excalidraw (E3 — LLM toolkit 갱신: PivotTable Sprint 1-5 + FlowBlock excalidraw 명세) | 100% | 2026-06-02 | [llm-toolkit-pivot-excalidraw/](llm-toolkit-pivot-excalidraw/) |
| pivot-sprint6-data-source (G1 — Pivot Table Sprint 6: DataSourceBlock 참조. source schema oneOf 확장 + viewer hydration + editor SourceKindPicker. engine pure 유지, 동일 query key 로 DataSourceBlockView 와 캐시 공유. payloadToRows pure helper 가 두 payload shape ({rows} / {headers,rows}) 모두 처리. 5 신규 unit + SSR harness QueryClientProvider wrap. typecheck clean, vitest 2426/2426 회귀 0) | 100% | 2026-06-03 | [pivot-sprint6-data-source/](pivot-sprint6-data-source/) |
| pivot-slicer-g2 (G2 — SlicerBlock 신규 + Pivot 의 boundSlicers cross-widget filter. zustand useSlicerStore (id → active set) + viewer hydration 단계에서 collectSlicerFilters 가 active 를 {field,op:'in'} 변환해 filters concat. engine 변경 0. single-select 재클릭=clear / multi=toggle / empty='All' (Excel semantic). inline rows 또는 data-source (Pivot 과 같은 query key 로 캐시 공유). SlicerBlockEditor 신규 + BoundSlicersPicker. BlockRenderer/InsertPalette 등록. 신규 10 unit (store 6 + collectSlicerFilters 4). typecheck clean, vitest 2436/2436 회귀 0. 36→37 block types) | 100% | 2026-06-03 | [pivot-slicer-g2/](pivot-slicer-g2/) |
| llm-viewer-guide (G3 — LLM viewer 가이드 신규. 외부 LLM 이 DocumentJSON 을 *읽을 때* (요약/Q&A) 사용. 37 block 별 사람-가독 요약 규칙 + PivotTable 6단계 요약 + Slicer 시점 의존성 + 인용 식별자 (ULID 8자 / §number) + 절대 하지 말 것 + 자기-체크리스트. docs/llm-viewer-guide.md 신규. RAG chunker source list 에 추가는 별도 cycle defer) | 100% | 2026-06-03 | [llm-viewer-guide/](llm-viewer-guide/) |
| i-track-chart-editor-kpi-slicers (I 트랙 — ChartBlockEditor picker UI (a) + KpiCardsBlock boundSlicers (b). (a) ChartSourcePanel 신규 — source kind radio (none/inline/data-source) + labelField + aggregations[] row repeater (field/agg/name 인라인 편집, add/remove) + DataSource picker. csv 모드 제외 (chart 와 직관 안 맞음), source=none 시 dimmed-but-editable 패턴. (b) KpiCardsBlock schema 에 block 레벨 source/filters/boundSlicers + items[i].compute (`{field, agg?, when?}`) 추가. useHydratedKpiCardsBlock hook 이 source 와 compute item 이 둘 다 있을 때만 hydrate, aggregateChartData 를 synthetic single-bucket (label='_') 로 재사용해 KpiCards 전용 aggregator 회피 (DRY). KpiSourcePanel editor UI + per-card compute 토글. 정적/동적 카드 공존 가능. sample 17 에 KpiCards 4 카드 추가 — cross-widget filter coverage 4/4 data widget 100% 완성. vitest 2460/2460 pass.) | 100% | 2026-06-04 | [i-track-chart-editor-kpi-slicers/](i-track-chart-editor-kpi-slicers/) |
| h-track-cleanup-chart-slicers (H 트랙 — G4 후속 cleanup + Chart boundSlicers + stale PDCA 정리. H0: 2026-05 archive 된 signup/bulk-import 의 active design/report 사본이 drift 되어 있어 truth 본문을 archive 로 옮기고 active 4 파일 git rm. H1 (`1c6d6e2`): Slicer/Timeline mount-시점 default hydration (useEffect 로 block.default → setActive, store entry 가 비어있을 때만) + palette i18n keys (ko/en 의 palette.pivot/slicer/timeline) + 17번째 sample (cross-widget-filter, Slicer+Timeline → Pivot+Table binding 데모). H2 (`6855285`): ChartBlock 에 5 개 optional schema 필드 (source / labelField / aggregations / filters / boundSlicers) + pivotEngine 의 새 export aggregateChartData (1D 그룹 + per-series agg, applyFilters 재사용) + ChartBlock viewer 의 useHydratedChartBlock hook (collectSlicerFilters + collectTimelineFilters 재사용, recharts + ECharts 자동 동작). source 미지정 시 100% back-compat. vitest 2454/2454 pass.) | 100% | 2026-06-04 | [h-track-cleanup-chart-slicers/](h-track-cleanup-chart-slicers/) |
| g4-defer-quad (G4 — defer 잔여 4 트랙 일괄. (1) chunker source list 에 viewer-guide.md 추가 → 15 RAG chunks 신규, (2) TableBlock.boundSlicers — collectSlicerFilters 시그니처 generic 화로 Pivot/Table 공유, sparse 모드 skip, BoundSlicersPicker 도 generic `<B>`. Chart/Kpi 는 source link 부재로 deferred, (3) TimelineBlock 신규 — 38번째 block. 두 range slider 로 [isoFrom, isoTo] 부분구간 → 같은 useSlicerStore 공유 (slicer/timeline 한 picker 로 binding) → collectTimelineFilters 가 `{op:'between'}` filter spec 으로 변환 → pivotEngine 의 between op (numeric + ISO date 양쪽 inclusive). PivotTable + Table viewer 모두 적용, (4) docs/llm-viewer-guide.en.md 영문 번역 — 9 챕터 1:1 + JSON skeleton/ULID/citation 패턴 verbatim. vitest 2454/2454 pass + pytest pass + typecheck clean + chunker --check 0) | 100% | 2026-06-03 | [g4-defer-quad/](g4-defer-quad/) |
